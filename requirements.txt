# Python dependencies for this project's training, evaluation, and
# benchmark-orchestration scripts (linear_p4, bnn_p4, tree_p4, test_scripts).

# Install (inside the P4 dev VM's venv):
#   pip3 install -r requirements.txt
#

numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
joblib>=1.0
scapy>=2.5
